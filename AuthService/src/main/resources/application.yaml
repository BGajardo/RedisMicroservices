server:
  port: 8081

spring:
  application:
    name: AuthService

  datasource:
    url: jdbc:postgresql://localhost:5432/authdb
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379